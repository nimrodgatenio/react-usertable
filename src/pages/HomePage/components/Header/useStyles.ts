import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
  appBar: {
    backgroundColor: "#2196f3",
  },
  title: {
    flexGrow: 1,
    textAlign: "center",
    fontWeight: "bold",
  },
  backButton: {
    marginLeft: "auto",
  },
}));
